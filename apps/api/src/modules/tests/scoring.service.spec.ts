import{ScoringService}from'./scoring.service';describe('ScoringService',()=>{const service=new ScoringService();it('maps objective raw scores to IELTS bands deterministically',()=>{expect(service.objective(40,40)).toBe(9);expect(service.objective(30,40)).toBe(7);expect(service.objective(0,40)).toBe(5)});it('marks subjective adapter output for examiner approval',async()=>{const score=await service.subjective('writing','word '.repeat(260));expect(score.band).toBeGreaterThanOrEqual(4);expect(score.feedback).toContain('examiner approval')})});
