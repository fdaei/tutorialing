'use client';

import {useMemo,useState} from 'react';
import {useSearchParams} from 'next/navigation';
import {useMutation,useQuery} from '@tanstack/react-query';
import Link from 'next/link';
import {ArrowLeft,ArrowRight,BadgeCheck,CalendarDays,Clock,ShieldCheck,Star,Users} from 'lucide-react';
import {api,publicApi,type PublicTeacher,ApiError,apiMessage} from '@/lib/api';
import {useTranslations} from '@/components/locale-provider';
import {localePath} from '@/lib/i18n';

type Slot={startsAt:string;endsAt:string;date:string;timezone:string;type:'trial'|'regular'};
type Payment={id:string;status?:string};

export default function Checkout(){
 const params=useSearchParams(),teacherId=params.get('teacher')??'',{locale}=useTranslations(),fa=locale==='fa',p=(href:string)=>localePath(href,locale),Arrow=fa?ArrowLeft:ArrowRight;
 const [lessonType,setLessonType]=useState<'trial'|'regular'>('trial'),[slot,setSlot]=useState<Slot|null>(null),[accepted,setAccepted]=useState(false),[week,setWeek]=useState(0),[discount,setDiscount]=useState('');
 const range=useMemo(()=>{const from=new Date();from.setSeconds(0,0);const to=new Date(from.getTime()+28*864e5);return{from:from.toISOString(),to:to.toISOString()}},[]);
 const teacher=useQuery({queryKey:['teacher-checkout',teacherId],queryFn:()=>publicApi<PublicTeacher>(`/teachers/${encodeURIComponent(teacherId)}`),enabled:!!teacherId});
 const slots=useQuery({queryKey:['slots',teacherId,lessonType,range.from,range.to],queryFn:()=>publicApi<Slot[]>(`/availability/${teacherId}/slots?from=${encodeURIComponent(range.from)}&to=${encodeURIComponent(range.to)}&type=${lessonType}`),enabled:!!teacherId});
 const checkout=useMutation({mutationFn:async()=>{if(!slot)throw new Error(fa?'یک زمان انتخاب کنید.':'Choose a time.');const booking=await api<{id:string}>('/bookings',{method:'POST',body:JSON.stringify({teacherId,startsAt:slot.startsAt,type:lessonType,policyAccepted:accepted,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone})});const payment=await api<Payment>('/payments',{method:'POST',body:JSON.stringify({purpose:'booking',referenceId:booking.id,walletAmount:0,discountCode:discount.trim()||undefined,idempotencyKey:crypto.randomUUID()})});if(payment.status==='PAID'){location.href=p('/payment/success');return}const gateway=await api<{url:string}>(`/payments/${payment.id}/gateway`,{method:'POST'});location.href=gateway.url}});
 const days=useMemo(()=>{const base=new Date();base.setHours(0,0,0,0);return Array.from({length:28},(_,index)=>{const date=new Date(base.getTime()+index*864e5);const key=date.toLocaleDateString('en-CA');return{date,key,slots:(slots.data??[]).filter(item=>localKey(item.startsAt)===key)}})},[slots.data]);
 const weekDays=days.slice(week*7,week*7+7),selectedDay=slot?localKey(slot.startsAt):(weekDays.find(day=>day.slots.length)?.key??weekDays[0]?.key),daySlots=weekDays.find(day=>day.key===selectedDay)?.slots??[];
 const t=teacher.data,trialPrice=t?.approvedTrialPrice??t?.trialPrice??0,regularPrice=t?.approvedRegularPrice??t?.regularPrice??0,price=lessonType==='trial'?trialPrice:regularPrice,duration=lessonType==='trial'?(t?.trialDuration??30):(t?.lessonDuration??60),money=(value:number)=>new Intl.NumberFormat(fa?'fa-IR':'en-US').format(value)+(fa?' تومان':' IRR');
 function chooseType(type:'trial'|'regular'){setLessonType(type);setSlot(null)}
 if(!teacherId)return <main className="mx-auto max-w-3xl px-6 py-24 text-center"><h1 className="text-3xl font-black">{fa?'مدرس انتخاب نشده است.':'No teacher was selected.'}</h1><Link href={p('/teachers')} className="mt-6 inline-block text-blue underline">{fa?'مشاهده مدرس‌ها':'Browse teachers'}</Link></main>;
 return <main className="min-h-screen bg-[#f7f8fc] pb-16"><header className="border-b hairline bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><Link href={t?p(`/teachers/${t.slug}`):p('/teachers')} className="flex items-center gap-2 text-sm font-bold text-muted"><Arrow size={17}/>{fa?'بازگشت به پروفایل مدرس':'Back to teacher profile'}</Link><strong className="latin text-lg">LingoSpeak</strong></div></header><div className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><div className="mb-7 rounded-3xl border hairline bg-white p-5 shadow-soft"><div className="flex flex-wrap items-center gap-4"><div className="brand-gradient grid size-16 place-items-center rounded-2xl text-2xl font-black text-white">{(fa?t?.nameFa:t?.nameEn)?.slice(0,1)??'L'}</div><div className="min-w-0 flex-1"><p className="flex items-center gap-2 text-xl font-black">{teacher.isLoading?(fa?'در حال دریافت مدرس…':'Loading teacher…'):(fa?t?.nameFa:t?.nameEn)}<BadgeCheck size={19} className="text-blue"/></p><div className="mt-2 flex flex-wrap gap-4 text-xs text-muted"><span className="flex items-center gap-1"><Star size={15} fill="#f5a623" className="text-[#f5a623]"/>{t?.rating??0} ({t?.reviewsCount??0})</span><span className="flex items-center gap-1"><Users size={15}/>{new Intl.NumberFormat(fa?'fa-IR':'en-US').format(t?.successfulClasses??0)} {fa?'کلاس موفق':'successful classes'}</span><span>{t?.languageLinks?.map(link=>`${link.language.flag??''} ${fa?link.language.nameFa:link.language.nameEn}`).join(' · ')}</span></div></div></div></div>{teacher.isError&&<ErrorBox text={fa?'اطلاعات مدرس دریافت نشد.':'Could not load the teacher.'}/>}<div className="grid gap-7 lg:grid-cols-[minmax(0,1fr)_360px]"><section className="space-y-6"><div className="rounded-3xl border hairline bg-white p-6"><h2 className="text-xl font-black">{fa?'نوع جلسه را انتخاب کنید':'Choose the lesson type'}</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><TypeCard active={lessonType==='trial'} onClick={()=>chooseType('trial')} title={fa?'جلسه آزمایشی':'Trial lesson'} detail={`${t?.trialDuration??30} ${fa?'دقیقه':'minutes'}`} price={money(trialPrice)}/><TypeCard active={lessonType==='regular'} disabled={!regularPrice} onClick={()=>chooseType('regular')} title={fa?'جلسه عادی':'Regular lesson'} detail={`${t?.lessonDuration??60} ${fa?'دقیقه':'minutes'}`} price={regularPrice?money(regularPrice):(fa?'پس از تأیید مدیریت':'Not currently available')}/></div></div><div className="rounded-3xl border hairline bg-white p-6"><div className="flex items-center justify-between gap-3"><div><h2 className="text-xl font-black">{fa?'روز و ساعت':'Date and time'}</h2><p className="mt-1 text-sm text-muted">{fa?'هر مرحله هفت روز را نشان می‌دهد.':'Each step shows seven days.'}</p></div><div className="flex gap-2"><button disabled={week===0} onClick={()=>{setWeek(value=>value-1);setSlot(null)}} className="grid size-10 place-items-center rounded-xl border hairline disabled:opacity-30" aria-label={fa?'هفته قبل':'Previous week'}>{fa?<ArrowRight/>:<ArrowLeft/>}</button><button disabled={week===3} onClick={()=>{setWeek(value=>value+1);setSlot(null)}} className="grid size-10 place-items-center rounded-xl border hairline disabled:opacity-30" aria-label={fa?'هفته بعد':'Next week'}>{fa?<ArrowLeft/>:<ArrowRight/>}</button></div></div>{slots.isLoading?<div className="mt-6 grid grid-cols-4 gap-3 sm:grid-cols-7">{Array.from({length:7},(_,i)=><div key={i} className="skeleton h-24 rounded-2xl"/>)}</div>:<><div className="mt-6 grid grid-cols-4 gap-2 sm:grid-cols-7">{weekDays.map(day=><button key={day.key} disabled={!day.slots.length} onClick={()=>setSlot(day.slots[0]??null)} className={`rounded-2xl border p-3 text-center transition ${selectedDay===day.key?'border-purple bg-lavender text-purple':'hairline'} disabled:opacity-35`}><span className="block text-xs text-muted">{new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{weekday:'short'}).format(day.date)}</span><strong className="mt-1 block text-lg">{new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{day:'numeric'}).format(day.date)}</strong><small className="mt-1 block">{day.slots.length?`${day.slots.length} ${fa?'زمان':'slots'}`:(fa?'پر':'Full')}</small></button>)}</div><div className="mt-6"><h3 className="flex items-center gap-2 font-black"><Clock size={18} className="text-purple"/>{selectedDay?new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{weekday:'long',month:'long',day:'numeric'}).format(new Date(`${selectedDay}T12:00:00`)):(fa?'زمانی موجود نیست':'No date available')}</h3>{daySlots.length?<div className="mt-4 grid grid-cols-3 gap-2 sm:grid-cols-4 md:grid-cols-6">{daySlots.map(item=><button key={item.startsAt} onClick={()=>setSlot(item)} className={`rounded-xl border px-3 py-3 font-bold transition ${slot?.startsAt===item.startsAt?'border-purple bg-purple text-white shadow-lg':'hairline hover:border-purple hover:text-purple'}`}>{new Intl.DateTimeFormat(fa?'fa-IR':'en-US',{hour:'2-digit',minute:'2-digit'}).format(new Date(item.startsAt))}</button>)}</div>:<p className="mt-4 rounded-2xl border border-dashed hairline p-6 text-center text-sm text-muted">{fa?'در این روز زمان آزادی وجود ندارد. روز دیگری را انتخاب کنید.':'No times are available on this day. Choose another day.'}</p>}</div></>}{slots.isError&&<ErrorBox text={fa?'زمان‌های آزاد دریافت نشدند. دوباره تلاش کنید.':'Could not load available times. Try again.'}/>}</div><label className="flex gap-3 rounded-3xl border hairline bg-white p-6"><input type="checkbox" checked={accepted} onChange={event=>setAccepted(event.target.checked)} className="mt-1 size-5 accent-purple"/><span><strong>{fa?'سیاست لغو را خوانده‌ام و می‌پذیرم.':'I have read and accept the cancellation policy.'}</strong><span className="mt-2 block text-sm leading-7 text-muted">{fa?(t?.policy?.titleFa??'مبلغ بازپرداخت براساس سیاست ثبت‌شده هنگام رزرو محاسبه می‌شود.'):(t?.policy?.titleEn??'Refund eligibility is calculated from the policy snapshot saved with the booking.')}</span></span></label></section><aside><div className="sticky top-6 rounded-3xl bg-navy p-7 text-white shadow-2xl"><h2 className="text-xl font-black">{fa?'خلاصه رزرو':'Booking summary'}</h2><Summary icon={<BadgeCheck/>} label={fa?'مدرس':'Teacher'} value={fa?t?.nameFa:t?.nameEn}/><Summary icon={<CalendarDays/>} label={fa?'تاریخ و ساعت':'Date and time'} value={slot?new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(slot.startsAt)):(fa?'هنوز انتخاب نشده':'Not selected')}/><Summary icon={<Clock/>} label={fa?'نوع و مدت':'Type and duration'} value={`${lessonType==='trial'?(fa?'آزمایشی':'Trial'):(fa?'عادی':'Regular')} · ${duration} ${fa?'دقیقه':'min'}`}/><div className="mt-6 border-t border-white/15 pt-5"><label className="text-xs text-white/60">{fa?'کد تخفیف':'Discount code'}</label><input value={discount} onChange={event=>setDiscount(event.target.value.toUpperCase())} className="mt-2 w-full rounded-xl border border-white/20 bg-white/10 px-4 py-3 outline-none placeholder:text-white/30" placeholder={fa?'اختیاری':'Optional'}/></div><div className="mt-6 flex items-end justify-between"><span className="text-sm text-white/60">{fa?'مبلغ نهایی':'Final amount'}</span><strong className="text-2xl">{money(price)}</strong></div>{checkout.isError&&<div role="alert" className="mt-5 rounded-xl bg-red-500/15 p-4 text-sm text-red-100">{apiMessage(checkout.error,fa?'رزرو انجام نشد. ممکن است این زمان هم‌اکنون رزرو یا مسدود شده باشد.':'Booking failed. The slot may have just been booked or blocked.')}</div>}<button disabled={!slot||!accepted||checkout.isPending||!price} onClick={()=>checkout.mutate()} className="brand-gradient mt-7 flex w-full items-center justify-center gap-2 rounded-xl py-4 font-black text-white disabled:opacity-40">{checkout.isPending?(fa?'در حال بررسی نهایی…':'Final availability check…'):(fa?'ادامه به پرداخت':'Continue to payment')}<ShieldCheck size={18}/></button><p className="mt-4 text-center text-xs leading-6 text-white/45">{fa?'آزاد بودن زمان پیش از ثبت نهایی دوباره در سرور بررسی می‌شود.':'The server rechecks availability before confirming the booking.'}</p></div></aside></div></div></main>
}
function localKey(value:string){const d=new Date(value),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return`${y}-${m}-${day}`}
function TypeCard({active,disabled,onClick,title,detail,price}:{active:boolean;disabled?:boolean;onClick:()=>void;title:string;detail:string;price:string}){return <button disabled={disabled} onClick={onClick} className={`rounded-2xl border p-5 text-start transition ${active?'border-purple bg-lavender ring-2 ring-purple/10':'hairline hover:border-purple'} disabled:opacity-40`}><strong className="block">{title}</strong><span className="mt-2 block text-sm text-muted">{detail}</span><span className="mt-4 block font-black text-blue">{price}</span></button>}
function Summary({icon,label,value}:{icon:React.ReactNode;label:string;value?:string}){return <div className="mt-5 flex gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-white/10 [&>svg]:size-4">{icon}</span><span><small className="block text-white/50">{label}</small><strong className="mt-1 block text-sm">{value||'—'}</strong></span></div>}
function ErrorBox({text}:{text:string}){return <div role="alert" className="mt-5 rounded-2xl border border-red-100 bg-red-50 p-4 text-red-800">{text}</div>}
