import Link from'next/link';import{requestLocale}from'@/lib/server-locale';import{localePath}from'@/lib/i18n';export default async function Pending(){const locale=await requestLocale(),fa=locale==='fa';return <main className="grid min-h-screen place-items-center p-6"><div className="max-w-md text-center"><div className="skeleton mx-auto h-16 w-16 rounded-full"/><h1 className="mt-6 text-4xl font-black">{fa?'پرداخت در حال بررسی است':'Payment is being reviewed'}</h1><p className="mt-4 leading-7 text-muted">{fa?'نتیجه پس از تأیید سمت سرور در پنل به‌روزرسانی می‌شود.':'The dashboard will update after server-side confirmation.'}</p><Link href={localePath('/dashboard/wallet',locale)} className="mt-7 inline-block font-bold text-blue underline">{fa?'مشاهده وضعیت':'View status'}</Link></div></main>}
