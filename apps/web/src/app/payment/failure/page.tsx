import Link from'next/link';import{requestLocale}from'@/lib/server-locale';import{localePath}from'@/lib/i18n';export default async function Failure(){const locale=await requestLocale(),fa=locale==='fa';return <main className="grid min-h-screen place-items-center p-6"><div className="max-w-md text-center"><span className="text-6xl text-red-500">×</span><h1 className="mt-5 text-4xl font-black">{fa?'پرداخت ناموفق بود':'Payment failed'}</h1><p className="mt-4 leading-7 text-muted">{fa?'وجهی قطعی نشده است. از پنل پرداخت‌ها می‌توانید دوباره تلاش کنید.':'No payment was finalized. You can try again from the payments dashboard.'}</p><Link href={localePath('/dashboard/wallet',locale)} className="mt-7 inline-block rounded-xl bg-navy px-7 py-4 font-black text-white">{fa?'بررسی پرداخت‌ها':'View payments'}</Link></div></main>}
