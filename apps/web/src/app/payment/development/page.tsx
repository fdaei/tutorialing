'use client';import{useSearchParams,useRouter}from'next/navigation';import{useMutation}from'@tanstack/react-query';import{publicApi}from'@/lib/api';import{useTranslations}from'@/components/locale-provider';import{localePath}from'@/lib/i18n';export default function DevelopmentPayment(){const authority=useSearchParams().get('authority')??'',router=useRouter(),{locale}=useTranslations(),fa=locale==='fa';const verify=useMutation({mutationFn:()=>publicApi(`/payments/callback?Authority=${encodeURIComponent(authority)}&Status=OK`),onSuccess:()=>router.replace(localePath('/payment/success',locale))});return <main className="grid min-h-screen place-items-center bg-[#f7f8fc] p-6"><div className="max-w-md rounded-4xl border hairline bg-white p-8 text-center shadow-soft"><p className="text-sm font-black text-purple">{fa?'درگاه توسعه':'Development gateway'}</p><h1 className="mt-4 text-3xl font-black">{fa?'شبیه‌ساز تأیید پرداخت':'Payment verification simulator'}</h1><p className="mt-4 leading-7 text-muted">{fa?'در محیط عملیاتی، کاربر به زرین‌پال منتقل می‌شود. این آداپتر همان callback سمت سرور را اجرا می‌کند.':'In production, the user is redirected to the payment provider. This adapter executes the same server callback.'}</p><button disabled={verify.isPending} onClick={()=>verify.mutate()} className="brand-gradient mt-7 w-full rounded-full py-4 font-black text-white">{verify.isPending?(fa?'در حال تأیید…':'Verifying…'):(fa?'تأیید پرداخت توسعه':'Confirm development payment')}</button>{verify.isError&&<p role="alert" className="mt-4 text-red-700">{fa?'تأیید ناموفق بود.':'Verification failed.'}</p>}</div></main>}
