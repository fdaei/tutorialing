import Link from'next/link';import{Check}from'lucide-react';import{requestLocale}from'@/lib/server-locale';import{localePath}from'@/lib/i18n';export default async function Success(){const locale=await requestLocale(),fa=locale==='fa';return <main className="grid min-h-screen place-items-center p-6"><div className="text-center"><span className="brand-gradient mx-auto grid h-20 w-20 place-items-center rounded-full text-white"><Check size={38}/></span><h1 className="mt-7 text-4xl font-black">{fa?'پرداخت تأیید شد':'Payment confirmed'}</h1><p className="mt-4 text-muted">{fa?'رزرو یا بسته شما در پنل قابل مشاهده است.':'Your booking or package is now available in the dashboard.'}</p><Link href={localePath('/dashboard',locale)} className="brand-gradient mt-7 inline-block rounded-xl px-7 py-4 font-black text-white">{fa?'رفتن به پنل':'Go to dashboard'}</Link></div></main>}
