'use client';import{useQuery}from'@tanstack/react-query';import Link from'next/link';import{AlertTriangle,ArrowLeft,ArrowRight,BookOpen,CalendarDays,CheckCircle2,Headphones,LifeBuoy,MessageCircle,Target}from'lucide-react';import{api}from'@/lib/api';import{PanelShell,studentNav}from'@/components/panel-shell';import{useTranslations}from'@/components/locale-provider';import{localePath}from'@/lib/i18n';
type Me={name?:string};type Booking={startsAt:string;status:string;meetingUrl?:string;teacher?:{nameFa?:string;nameEn?:string}};type Attempt={status:string;overallBand?:number};
export default function Dashboard(){const{locale}=useTranslations(),fa=locale==='fa',p=(href:string)=>localePath(href,locale),Arrow=fa?ArrowLeft:ArrowRight,me=useQuery({queryKey:['me'],queryFn:()=>api<Me>('/users/me')}),bookings=useQuery({queryKey:['bookings'],queryFn:()=>api<Booking[]>('/bookings/me')}),attempts=useQuery({queryKey:['attempt-history'],queryFn:()=>api<Attempt[]>('/tests/attempts/history')});const next=bookings.data?.find(b=>new Date(b.startsAt)>new Date()&&b.status==='CONFIRMED'),last=attempts.data?.find(a=>a.status==='APPROVED'),assessmentDone=Boolean(last),hasClass=Boolean(next),teacherName=fa?next?.teacher?.nameFa:next?.teacher?.nameEn;const journey=fa?[[CheckCircle2,'ساخت حساب کاربری','تکمیل‌شده',true],[Target,'ارزیابی سطح زبان',assessmentDone?'تکمیل‌شده':'مرحله بعدی شما',assessmentDone],[BookOpen,'انتخاب مدرس',bookings.data?.length?'فعال':'پس از تعیین سطح',Boolean(bookings.data?.length)],[Headphones,'شروع کلاس‌ها',hasClass?'رزرو شده':'در انتظار رزرو',hasClass]]:[[CheckCircle2,'Create your account','Completed',true],[Target,'Assess your level',assessmentDone?'Completed':'Your next step',assessmentDone],[BookOpen,'Choose a teacher',bookings.data?.length?'Active':'After assessment',Boolean(bookings.data?.length)],[Headphones,'Start classes',hasClass?'Booked':'Waiting for booking',hasClass]];return <PanelShell title="پنل زبان‌آموز" items={studentNav}><section className="soft-gradient panel-card relative overflow-hidden p-7 md:p-10"><div className="relative grid items-center gap-8 lg:grid-cols-[1fr_320px]"><div><span className="inline-flex rounded-full bg-white/75 px-4 py-2 text-xs font-bold text-purple">{fa?'هر روز یک قدم رو به جلو':'One step forward every day'}</span><h1 className="mt-7 text-5xl font-black leading-tight md:text-6xl">{fa?`سلام ${me.data?.name??'زبان‌آموز'}؛`:`Hi ${me.data?.name??'there'},`}<br/><span className="brand-text">{fa?'در مسیر بمان.':'stay in flow.'}</span></h1><p className="mt-5 max-w-xl leading-8 text-muted">{fa?'برنامه‌ات را ادامه بده، کلاس بعدی و نتیجه آزمون‌ها را ببین و از همین‌جا با مدرس و پشتیبانی در ارتباط باش.':'Continue your plan, view your next class and test results, and stay connected with your teacher and support.'}</p><Link href={p(assessmentDone?'/dashboard/plan':'/placement')} className="brand-gradient brand-glow mt-7 inline-flex items-center gap-3 rounded-xl px-7 py-4 font-black text-white">{assessmentDone?(fa?'ادامه برنامه یادگیری':'Continue learning plan'):(fa?'شروع تعیین سطح':'Start placement test')}<Arrow size={19}/></Link></div><div className="grid grid-cols-2 gap-3"><Stat label={fa?'آزمون‌ها':'Tests'} value={attempts.data?.length??0}/><Stat label={fa?'کلاس‌ها':'Classes'} value={bookings.data?.length??0}/>{last&&<div className="panel-card col-span-2 p-5"><small className="block text-muted">{fa?'آخرین نمره تأییدشده':'Latest approved band'}</small><strong className="latin mt-2 block text-3xl text-purple">{last.overallBand}</strong></div>}</div></div></section>
<section className="mt-6 grid gap-4 md:grid-cols-2"><Link href={p(hasClass?'/dashboard/classes':'/matching')} className="panel-card lift flex items-center gap-4 p-6"><span className="grid size-14 place-items-center rounded-full bg-lavender text-purple"><CalendarDays/></span><span className="flex-1"><small className="font-bold text-purple">{hasClass?(fa?'کلاس بعدی شما':'Your next class'):(fa?'مرحله بعدی':'Next step')}</small><strong className="mt-1 block text-xl">{hasClass?(fa?`کلاس با ${teacherName??'مدرس'}`:`Class with ${teacherName??'your teacher'}`):(fa?'انتخاب مدرس مناسب':'Choose the right teacher')}</strong><small className="mt-1 block text-muted">{hasClass&&next?new Intl.DateTimeFormat(fa?'fa-IR':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(next.startsAt)):(fa?'پیشنهادهای هوشمند را مشاهده کنید':'View your smart recommendations')}</small></span><Arrow/></Link><Link href={p(assessmentDone?'/dashboard/tests':'/placement')} className="panel-card lift flex items-center gap-4 p-6"><span className={`grid size-14 place-items-center rounded-full ${assessmentDone?'bg-emerald-50 text-emerald-600':'bg-orange-50 text-orange-500'}`}>{assessmentDone?<CheckCircle2/>:<AlertTriangle/>}</span><span className="flex-1"><small className="font-bold text-orange-500">{fa?'نیازمند توجه':'Needs attention'}</small><strong className="mt-1 block text-xl">{assessmentDone?(fa?'مشاهده نتیجه تعیین سطح':'View placement result'):(fa?'تکمیل تعیین سطح':'Complete placement test')}</strong><small className="mt-1 block text-muted">{assessmentDone?(fa?'نتیجه شما آماده است':'Your result is ready'):(fa?'برای شروع مسیر، سطح خود را مشخص کنید':'Assess your level to begin')}</small></span><Arrow/></Link></section>
<section className="mt-6 grid gap-5 xl:grid-cols-[1fr_330px]"><article className="panel-card p-6 md:p-8"><div className="flex items-center justify-between"><h2 className="text-2xl font-black">{fa?'مسیر یادگیری من':'My learning journey'}</h2><Link href={p('/dashboard/plan')} className="text-sm font-bold text-blue">{fa?'مشاهده همه':'View all'}</Link></div><div className="mt-6 divide-y hairline">{journey.map(([I,title,status,done])=>{const Icon=I as typeof Target;return <div key={String(title)} className="flex items-center gap-4 py-5"><span className={`grid size-11 place-items-center rounded-xl ${done?'bg-[#eef2ff] text-blue':'bg-[#f4f5f8] text-muted'}`}><Icon size={21}/></span><p className="flex-1"><strong>{String(title)}</strong><small className="mt-1 block text-muted">{String(status)}</small></p>{done?<span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-600">{fa?'انجام شد':'Done'}</span>:<Link href={p(String(title).includes('سطح')||String(title).includes('level')?'/placement':'/matching')} className="rounded-xl border border-blue px-4 py-2 text-sm font-bold text-blue">{fa?'شروع':'Start'}</Link>}</div>})}</div></article><aside className="relative overflow-hidden rounded-[24px] bg-[#111b4a] p-7 text-white shadow-brand"><span className="grid size-14 place-items-center rounded-full bg-purple/25 text-violet"><LifeBuoy/></span><h2 className="mt-10 text-3xl font-black">{fa?'نیاز به راهنمایی دارید؟':'Need a hand?'}</h2><p className="mt-4 text-sm leading-7 text-white/60">{fa?'تیم پشتیبانی آماده است تا در مسیر یادگیری همراه شما باشد.':'Our support team is ready to help throughout your learning journey.'}</p><Link href={p('/dashboard/tickets')} className="brand-gradient mt-8 flex items-center justify-center gap-3 rounded-xl px-5 py-4 font-black"><MessageCircle size={19}/>{fa?'ایجاد تیکت جدید':'Create a ticket'}</Link></aside></section></PanelShell>}
function Stat({label,value}:{label:string;value:number}){return <div className="panel-card p-5"><small className="block text-muted">{label}</small><strong className="latin mt-2 block text-3xl">{value}</strong></div>}
