'use client';

import {useEffect,useState} from 'react';
import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';
import {CalendarDays,Clock,Plus,Trash2} from 'lucide-react';
import {api,apiField,apiMessage} from '@/lib/api';
import {useTranslations} from './locale-provider';
import {JalaliDateTimePicker} from './jalali-date-time-picker';

type Rule={id?:string;weekday:number;startMinute:number;endMinute:number;timezone:string;lessonDuration?:number;breakMinutes?:number};
type Override={id:string;date:string;available:boolean;startMinute?:number;endMinute?:number;reason?:string};
type Block={id:string;startsAt:string;endsAt:string;reason?:string;adminCreated:boolean};
type Availability={timezone:string;rules:Rule[];overrides:Override[];blocks:Block[]};
const weekdayFa=['یکشنبه','دوشنبه','سه‌شنبه','چهارشنبه','پنجشنبه','جمعه','شنبه'];
const weekdayEn=['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];

export function TeacherAvailabilityManager(){
 const {locale}=useTranslations(),fa=locale==='fa',qc=useQueryClient(),query=useQuery({queryKey:['availability-me'],queryFn:()=>api<Availability>('/availability/me')});
 const [rules,setRules]=useState<Rule[]>([]),[blockStart,setBlockStart]=useState<Date|null>(null),[blockEnd,setBlockEnd]=useState<Date|null>(null),[blockReason,setBlockReason]=useState(''),[overrideDate,setOverrideDate]=useState<Date|null>(null),[overrideAvailable,setOverrideAvailable]=useState(false),[overrideStart,setOverrideStart]=useState('09:00'),[overrideEnd,setOverrideEnd]=useState('17:00'),[overrideReason,setOverrideReason]=useState('');
 useEffect(()=>{if(query.data)setRules(query.data.rules)},[query.data]);
 const invalidate=async()=>{await Promise.all([qc.invalidateQueries({queryKey:['availability-me']}),qc.invalidateQueries({queryKey:['slots']})])};
 const saveRules=useMutation({mutationFn:()=>api('/availability/me/rules',{method:'PUT',body:JSON.stringify({rules:rules.map(rule=>({...rule,timezone:rule.timezone||query.data?.timezone||'Asia/Tehran'}))})}),onSuccess:invalidate});
 const addBlock=useMutation({mutationFn:()=>api('/availability/me/blocks',{method:'POST',body:JSON.stringify({startsAt:blockStart?.toISOString(),endsAt:blockEnd?.toISOString(),reason:blockReason||undefined})}),onSuccess:()=>{setBlockStart(null);setBlockEnd(null);setBlockReason('');invalidate()}});
 const removeBlock=useMutation({mutationFn:(id:string)=>api(`/availability/me/blocks/${id}`,{method:'DELETE'}),onSuccess:invalidate});
 const addOverride=useMutation({mutationFn:()=>{if(!overrideDate)throw new Error(fa?'تاریخ را انتخاب کنید.':'Choose a date.');const [sh=0,sm=0]=overrideStart.split(':').map(Number),[eh=0,em=0]=overrideEnd.split(':').map(Number);return api('/availability/me/overrides',{method:'POST',body:JSON.stringify({date:dateOnlyIso(overrideDate),available:overrideAvailable,startMinute:overrideAvailable?sh*60+sm:undefined,endMinute:overrideAvailable?eh*60+em:undefined,reason:overrideReason||undefined})})},onSuccess:()=>{setOverrideDate(null);setOverrideReason('');invalidate()}});
 const removeOverride=useMutation({mutationFn:(id:string)=>api(`/availability/me/overrides/${id}`,{method:'DELETE'}),onSuccess:invalidate});
 function addRule(){setRules(current=>[...current,{weekday:1,startMinute:9*60,endMinute:17*60,timezone:query.data?.timezone||'Asia/Tehran',lessonDuration:60,breakMinutes:15}])}
 function updateRule(index:number,patch:Partial<Rule>){setRules(current=>current.map((row,i)=>i===index?{...row,...patch}:row))}
 if(query.isLoading)return <div className="grid gap-4"><div className="skeleton h-64 rounded-3xl"/><div className="skeleton h-64 rounded-3xl"/></div>;
 if(query.isError)return <ErrorBox message={apiMessage(query.error,fa?'برنامه زمانی دریافت نشد.':'Could not load availability.')} retry={()=>query.refetch()}/>;
 return <div className="grid gap-6"><section className="rounded-3xl border hairline bg-white p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-2xl font-black">{fa?'برنامه هفتگی تکرارشونده':'Recurring weekly schedule'}</h2><p className="mt-2 text-sm text-muted">{fa?'مدت کلاس و فاصله بین کلاس‌ها برای هر بازه مستقل است. هم‌پوشانی در سرور رد می‌شود.':'Lesson length and break are stored per range. Server-side validation rejects overlaps.'}</p></div><button onClick={addRule} className="flex items-center gap-2 rounded-xl border border-purple px-4 py-3 font-bold text-purple"><Plus size={18}/>{fa?'افزودن بازه':'Add range'}</button></div><div className="mt-6 grid gap-3">{rules.length?rules.map((rule,index)=><div key={rule.id??index} className="grid gap-3 rounded-2xl border hairline p-4 md:grid-cols-[1.2fr_1fr_1fr_.8fr_.8fr_auto]"><select value={rule.weekday} onChange={event=>updateRule(index,{weekday:Number(event.target.value)})} className="input">{(fa?weekdayFa:weekdayEn).map((day,i)=><option value={i} key={day}>{day}</option>)}</select><TimeInput value={rule.startMinute} onChange={startMinute=>updateRule(index,{startMinute})}/><TimeInput value={rule.endMinute} onChange={endMinute=>updateRule(index,{endMinute})}/><NumberInput value={rule.lessonDuration??60} min={15} max={240} label={fa?'مدت':'Duration'} onChange={lessonDuration=>updateRule(index,{lessonDuration})}/><NumberInput value={rule.breakMinutes??15} min={0} max={120} label={fa?'فاصله':'Break'} onChange={breakMinutes=>updateRule(index,{breakMinutes})}/><button aria-label={fa?'حذف بازه':'Remove range'} onClick={()=>setRules(current=>current.filter((_,i)=>i!==index))} className="grid size-11 place-items-center rounded-xl text-red-600 hover:bg-red-50"><Trash2 size={18}/></button></div>):<p className="rounded-2xl border border-dashed hairline p-8 text-center text-muted">{fa?'هنوز برنامه هفتگی ثبت نشده است.':'No weekly schedule has been added.'}</p>}</div>{saveRules.isError&&<InlineError error={saveRules.error} field="rules" fa={fa}/>}<button onClick={()=>saveRules.mutate()} disabled={saveRules.isPending} className="brand-gradient mt-5 rounded-xl px-6 py-3 font-black text-white disabled:opacity-50">{saveRules.isPending?(fa?'در حال ذخیره…':'Saving…'):(fa?'ذخیره برنامه هفتگی':'Save weekly schedule')}</button>{saveRules.isSuccess&&<Success text={fa?'برنامه هفتگی ذخیره شد.':'Weekly schedule saved.'}/>}</section><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-3xl border hairline bg-white p-6"><h2 className="text-2xl font-black">{fa?'استثنای یک روز':'Single-day exception'}</h2><p className="mt-2 text-sm text-muted">{fa?'این استثنا جای برنامه هفتگی همان روز را می‌گیرد.':'This exception replaces the weekly schedule for that date.'}</p><div className="mt-5 grid gap-4"><JalaliDateTimePicker value={overrideDate} onChange={setOverrideDate} withTime={false}/><label className="flex items-center gap-3 rounded-xl border hairline p-4"><input type="checkbox" checked={overrideAvailable} onChange={event=>setOverrideAvailable(event.target.checked)} className="size-5 accent-purple"/><span>{fa?'به‌جای تعطیلی کامل، یک بازه آزاد اضافه شود.':'Add an available range instead of closing the whole day.'}</span></label>{overrideAvailable&&<div className="grid grid-cols-2 gap-3"><label><span className="mb-2 block text-xs text-muted">{fa?'شروع':'Start'}</span><input type="time" value={overrideStart} onChange={event=>setOverrideStart(event.target.value)} className="input"/></label><label><span className="mb-2 block text-xs text-muted">{fa?'پایان':'End'}</span><input type="time" value={overrideEnd} onChange={event=>setOverrideEnd(event.target.value)} className="input"/></label></div>}<input value={overrideReason} onChange={event=>setOverrideReason(event.target.value)} className="input" placeholder={fa?'دلیل اختیاری':'Optional reason'}/><button disabled={addOverride.isPending||!overrideDate} onClick={()=>addOverride.mutate()} className="brand-gradient rounded-xl py-3 font-black text-white disabled:opacity-50">{fa?'ذخیره استثنا':'Save exception'}</button>{addOverride.isError&&<InlineError error={addOverride.error} field="endMinute" fa={fa}/>}</div><div className="mt-6 grid gap-3">{query.data?.overrides.map(item=><article key={item.id} className="flex items-center justify-between gap-3 rounded-2xl bg-[#f7f8fc] p-4"><div><strong>{formatDate(item.date,fa)}</strong><p className="mt-1 text-sm text-muted">{item.available?`${minutes(item.startMinute)}–${minutes(item.endMinute)}`:(fa?'تعطیل کامل':'Closed all day')}{item.reason?` · ${item.reason}`:''}</p></div><button onClick={()=>removeOverride.mutate(item.id)} className="text-red-600"><Trash2 size={18}/></button></article>)}</div></section><section className="rounded-3xl border hairline bg-white p-6"><h2 className="text-2xl font-black">{fa?'مسدودکردن بازه زمانی':'Block a time period'}</h2><p className="mt-2 text-sm text-muted">{fa?'بازه مسدود فوراً از نوبت‌های عمومی حذف و رزرو آن در Backend رد می‌شود.':'The period is immediately removed from public slots and rejected by the booking backend.'}</p><div className="mt-5 grid gap-4"><JalaliDateTimePicker value={blockStart} onChange={setBlockStart}/><JalaliDateTimePicker value={blockEnd} onChange={setBlockEnd} minDate={blockStart??new Date()}/><input value={blockReason} onChange={event=>setBlockReason(event.target.value)} className="input" placeholder={fa?'دلیل اختیاری':'Optional reason'}/><button disabled={addBlock.isPending||!blockStart||!blockEnd} onClick={()=>addBlock.mutate()} className="brand-gradient rounded-xl py-3 font-black text-white disabled:opacity-50">{fa?'ثبت مسدودی':'Create block'}</button>{addBlock.isError&&<InlineError error={addBlock.error} field="endsAt" fa={fa}/>} {addBlock.isSuccess&&<Success text={fa?'بازه مسدود شد و فهرست به‌روز شد.':'The period was blocked and the list refreshed.'}/>}</div><div className="mt-6 grid gap-3">{query.data?.blocks.length?query.data.blocks.map(item=><article key={item.id} className="flex items-center justify-between gap-3 rounded-2xl bg-[#f7f8fc] p-4"><div><strong>{formatDateTime(item.startsAt,fa)} — {formatDateTime(item.endsAt,fa)}</strong><p className="mt-1 text-sm text-muted">{item.reason||(fa?'بدون دلیل ثبت‌شده':'No reason supplied')}{item.adminCreated?` · ${fa?'توسط ادمین':'Admin'}`:''}</p></div><button disabled={removeBlock.isPending} onClick={()=>removeBlock.mutate(item.id)} className="text-red-600"><Trash2 size={18}/></button></article>):<p className="rounded-2xl border border-dashed hairline p-6 text-center text-sm text-muted">{fa?'مسدودی فعالی وجود ندارد.':'There are no active blocked periods.'}</p>}</div></section></div><style jsx global>{`.input{width:100%;border:1px solid rgba(16,29,53,.14);border-radius:.9rem;padding:.8rem 1rem;background:white;outline:none}.input:focus{border-color:#7257d9;box-shadow:0 0 0 4px rgba(114,87,217,.08)}`}</style></div>
}
function TimeInput({value,onChange}:{value:number;onChange:(value:number)=>void}){return <input type="time" value={`${String(Math.floor(value/60)).padStart(2,'0')}:${String(value%60).padStart(2,'0')}`} onChange={event=>{const[h=0,m=0]=event.target.value.split(':').map(Number);onChange(h*60+m)}} className="input"/>}
function NumberInput({value,min,max,label,onChange}:{value:number;min:number;max:number;label:string;onChange:(value:number)=>void}){return <label><span className="mb-1 block text-[10px] text-muted">{label}</span><input type="number" min={min} max={max} value={value} onChange={event=>onChange(Number(event.target.value))} className="input"/></label>}
function minutes(value?:number){if(value==null)return'—';return`${String(Math.floor(value/60)).padStart(2,'0')}:${String(value%60).padStart(2,'0')}`}
function dateOnlyIso(date:Date){return new Date(Date.UTC(date.getFullYear(),date.getMonth(),date.getDate())).toISOString()}
function formatDate(value:string,fa:boolean){return new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{dateStyle:'full',timeZone:'UTC'}).format(new Date(value))}
function formatDateTime(value:string,fa:boolean){return new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value))}
function InlineError({error,field,fa}:{error:unknown;field:string;fa:boolean}){return <p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-800">{apiMessage(error,fa?'ذخیره ناموفق بود.':'Could not save.')} {apiField(error,field)}</p>}
function Success({text}:{text:string}){return <p role="status" className="mt-3 rounded-xl bg-green-50 p-3 text-sm text-green-800">{text}</p>}
function ErrorBox({message,retry}:{message:string;retry:()=>void}){return <div role="alert" className="rounded-3xl bg-red-50 p-6 text-red-800">{message} <button onClick={retry} className="font-bold underline">Retry</button></div>}
