'use client';

import {useEffect,useState} from 'react';
import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';
import {CheckCircle2,Clock3,FileAudio,PenLine,RotateCcw,UserRound} from 'lucide-react';
import {api,apiMessage,type ItemsPage} from '@/lib/api';
import {useTranslations} from './locale-provider';

type ReviewStatus='PENDING'|'IN_REVIEW'|'APPROVED'|'NEEDS_REVISION';
type QueueAnswer={
 id:string;textValue?:string;fileId?:string;reviewStatus:ReviewStatus;reviewBand?:number;reviewCriteria?:Record<string,number>;feedbackFa?:string;feedbackEn?:string;reviewedAt?:string;
 reviewer?:{name?:string};question:{prompt:{fa?:string;en?:string};type:string;section:{skill:string;title:string}};
 attempt:{submittedAt:string;user:{name?:string;phone:string};test:{titleFa:string;titleEn:string;language:{nameFa:string;nameEn:string;nativeName:string;flag?:string}}};
};
type Queue={items:QueueAnswer[];pagination:{page:number;pageSize:number;total:number;pages:number}};
const tabs=[['pending','در انتظار بررسی','Pending'],['in_review','در حال بررسی','In review'],['reviewed','بررسی‌شده','Reviewed'],['needs_revision','نیازمند اصلاح','Needs revision']] as const;

export function ExaminerReviewManager(){
 const {locale}=useTranslations(),fa=locale==='fa',qc=useQueryClient(),[tab,setTab]=useState<(typeof tabs)[number][0]>('pending'),[page,setPage]=useState(1),[active,setActive]=useState<QueueAnswer|null>(null);
 const queue=useQuery({queryKey:['examiner-queue',tab,page],queryFn:()=>api<Queue>(`/examiner/tests/queue?status=${tab}&page=${page}&pageSize=20`)});
 const claim=useMutation({mutationFn:(id:string)=>api<QueueAnswer>(`/examiner/tests/answers/${id}/claim`,{method:'POST'}),onSuccess:answer=>{setActive(answer);qc.invalidateQueries({queryKey:['examiner-queue']})}});
 useEffect(()=>{setPage(1);setActive(null)},[tab]);
 return <div className="grid gap-6"><section className="rounded-3xl border hairline bg-white p-5"><div className="flex gap-2 overflow-x-auto">{tabs.map(([value,labelFa,labelEn])=><button key={value} onClick={()=>setTab(value)} className={`shrink-0 rounded-xl px-4 py-3 text-sm font-bold ${tab===value?'brand-gradient text-white':'bg-[#f5f6fa] text-muted'}`}>{fa?labelFa:labelEn}</button>)}</div></section><div className="grid gap-6 xl:grid-cols-[380px_minmax(0,1fr)]"><section className="rounded-3xl border hairline bg-white p-4"><div className="flex items-center justify-between px-2"><h2 className="text-xl font-black">{fa?'پاسخ‌ها':'Answers'}</h2><span className="rounded-full bg-lavender px-3 py-1 text-xs font-black text-purple">{queue.data?.pagination.total??0}</span></div>{queue.isLoading?<div className="mt-4 grid gap-3">{Array.from({length:4},(_,i)=><div key={i} className="skeleton h-36 rounded-2xl"/>)}</div>:queue.isError?<ErrorBox message={apiMessage(queue.error,fa?'صف بررسی دریافت نشد.':'Could not load the review queue.')} retry={()=>queue.refetch()}/>:queue.data?.items.length?<div className="mt-4 grid gap-3">{queue.data.items.map(item=><button key={item.id} onClick={()=>setActive(item)} className={`rounded-2xl border p-4 text-start ${active?.id===item.id?'border-purple bg-lavender/40':'hairline hover:border-purple'}`}><div className="flex items-start justify-between gap-3"><strong>{fa?item.attempt.user.name??'زبان‌آموز':item.attempt.user.name??'Learner'}</strong><Status status={item.reviewStatus} fa={fa}/></div><p className="mt-2 text-sm text-muted">{item.attempt.test.language.flag} {fa?item.attempt.test.language.nameFa:item.attempt.test.language.nameEn} · {item.question.section.title}</p><p className="mt-2 line-clamp-2 text-sm">{(fa?item.question.prompt.fa:item.question.prompt.en)??item.question.prompt.fa??item.question.prompt.en}</p><p className="mt-3 flex items-center gap-2 text-xs text-muted"><Clock3 size={14}/>{new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(item.attempt.submittedAt))}</p></button>)}</div>:<Empty text={fa?'پاسخی در این تب وجود ندارد.':'There are no answers in this tab.'}/>}<div className="mt-4 flex items-center justify-between"><button disabled={page<=1} onClick={()=>setPage(value=>value-1)} className="rounded-xl border hairline px-4 py-2 disabled:opacity-30">{fa?'قبلی':'Previous'}</button><span className="text-sm text-muted">{page} / {Math.max(1,queue.data?.pagination.pages??1)}</span><button disabled={page>=(queue.data?.pagination.pages??1)} onClick={()=>setPage(value=>value+1)} className="rounded-xl border hairline px-4 py-2 disabled:opacity-30">{fa?'بعدی':'Next'}</button></div></section><section className="rounded-3xl border hairline bg-white p-6">{active?<ReviewEditor key={active.id} answer={active} fa={fa} tab={tab} claim={()=>claim.mutate(active.id)} claiming={claim.isPending} done={()=>{setActive(null);qc.invalidateQueries({queryKey:['examiner-queue']})}}/>:<Empty text={fa?'یک پاسخ را انتخاب کنید تا متن، صدا و فرم ارزیابی نمایش داده شود.':'Select an answer to view its text, audio, and review form.'}/>}</section></div></div>
}
function ReviewEditor({answer,fa,tab,claim,claiming,done}:{answer:QueueAnswer;fa:boolean;tab:string;claim:()=>void;claiming:boolean;done:()=>void}){
 const [band,setBand]=useState(answer.reviewBand??6.5),[feedbackFa,setFeedbackFa]=useState(answer.feedbackFa??''),[feedbackEn,setFeedbackEn]=useState(answer.feedbackEn??''),[status,setStatus]=useState<'APPROVED'|'NEEDS_REVISION'>(answer.reviewStatus==='NEEDS_REVISION'?'NEEDS_REVISION':'APPROVED'),[criteria,setCriteria]=useState<Record<string,number>>(answer.reviewCriteria??defaultCriteria(answer.question.section.skill));
 const review=useMutation({mutationFn:()=>api('/examiner/tests/answers/review',{method:'POST',body:JSON.stringify({answerId:answer.id,band,criteria,feedbackFa,feedbackEn,status})}),onSuccess:done});
 const canEdit=answer.reviewStatus==='IN_REVIEW'||tab==='in_review';
 return <div><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="flex items-center gap-2 text-sm font-bold text-purple"><UserRound size={17}/>{fa?answer.attempt.user.name??'زبان‌آموز':answer.attempt.user.name??'Learner'}</p><h2 className="mt-2 text-2xl font-black">{fa?answer.attempt.test.titleFa:answer.attempt.test.titleEn}</h2><p className="mt-2 text-sm text-muted">{answer.attempt.test.language.flag} {answer.attempt.test.language.nativeName} · {answer.question.section.skill}</p></div><Status status={answer.reviewStatus} fa={fa}/></div><div className="mt-6 rounded-2xl bg-[#f7f8fc] p-5"><p className="font-black">{(fa?answer.question.prompt.fa:answer.question.prompt.en)??answer.question.prompt.fa??answer.question.prompt.en}</p>{answer.textValue&&<div className="mt-4 whitespace-pre-wrap rounded-xl bg-white p-5 leading-8">{answer.textValue}</div>}{answer.fileId&&<ReviewAudio fileId={answer.fileId} fa={fa}/>}</div>{!canEdit&&['PENDING','NEEDS_REVISION'].includes(answer.reviewStatus)&&<button onClick={claim} disabled={claiming} className="brand-gradient mt-6 rounded-xl px-6 py-3 font-black text-white disabled:opacity-50">{claiming?(fa?'در حال دریافت…':'Claiming…'):(fa?'شروع بررسی این پاسخ':'Claim and start review')}</button>}{canEdit&&<div className="mt-7"><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block font-black">{fa?'نمره':'Band / score'}</span><input type="number" min={0} max={9} step={.5} value={band} onChange={event=>setBand(Number(event.target.value))} className="input"/></label><label><span className="mb-2 block font-black">{fa?'وضعیت نتیجه':'Review outcome'}</span><select value={status} onChange={event=>setStatus(event.target.value as typeof status)} className="input"><option value="APPROVED">{fa?'تأییدشده':'Approved'}</option><option value="NEEDS_REVISION">{fa?'نیازمند اصلاح':'Needs revision'}</option></select></label></div><h3 className="mt-6 font-black">{fa?'معیارهای ارزیابی':'Assessment criteria'}</h3><div className="mt-3 grid gap-3 sm:grid-cols-2">{Object.entries(criteria).map(([key,value])=><label key={key}><span className="mb-2 block text-xs text-muted">{criterionLabel(key,fa)}</span><input type="number" min={0} max={9} step={.5} value={value} onChange={event=>setCriteria(current=>({...current,[key]:Number(event.target.value)}))} className="input"/></label>)}</div><label className="mt-5 block"><span className="mb-2 block font-black">{fa?'بازخورد فارسی':'Persian feedback'}</span><textarea value={feedbackFa} onChange={event=>setFeedbackFa(event.target.value)} className="input min-h-32" placeholder={fa?'مشکل، دلیل و روش اصلاح را دقیق توضیح دهید.':'Explain the issue, why it matters, and how to fix it.'}/></label><label className="mt-5 block"><span className="mb-2 block font-black">{fa?'بازخورد انگلیسی':'English feedback'}</span><textarea dir="ltr" value={feedbackEn} onChange={event=>setFeedbackEn(event.target.value)} className="input min-h-32" placeholder="Explain the issue, why it matters, and how the learner can improve."/></label>{review.isError&&<p role="alert" className="mt-4 rounded-xl bg-red-50 p-4 text-red-800">{apiMessage(review.error,fa?'ثبت بررسی ناموفق بود.':'Could not save the review.')}</p>}<button disabled={review.isPending||!feedbackFa.trim()||!feedbackEn.trim()} onClick={()=>review.mutate()} className="brand-gradient mt-5 flex items-center gap-2 rounded-xl px-6 py-3 font-black text-white disabled:opacity-40"><CheckCircle2 size={18}/>{review.isPending?(fa?'در حال نهایی‌سازی…':'Finalizing…'):(fa?'ثبت بررسی و به‌روزرسانی نتیجه':'Save review and update result')}</button></div>}<style jsx global>{`.input{width:100%;border:1px solid rgba(16,29,53,.14);border-radius:.9rem;padding:.85rem 1rem;background:white;outline:none}.input:focus{border-color:#7257d9;box-shadow:0 0 0 4px rgba(114,87,217,.08)}`}</style></div>
}
function ReviewAudio({fileId,fa}:{fileId:string;fa:boolean}){const query=useQuery({queryKey:['review-audio',fileId],queryFn:()=>api<{url:string}>(`/files/${fileId}/download`)});return <div className="mt-4 rounded-xl bg-white p-4"><p className="mb-3 flex items-center gap-2 text-sm font-bold"><FileAudio size={17}/>{fa?'پاسخ صوتی ذخیره‌شده':'Saved audio answer'}</p>{query.isLoading?<div className="skeleton h-12 rounded-xl"/>:query.data?<audio controls preload="metadata" className="w-full" src={query.data.url}/>:<p className="text-sm text-red-700">{fa?'دریافت فایل صوتی ناموفق بود.':'Could not load the audio file.'}</p>}</div>}
function Status({status,fa}:{status:ReviewStatus;fa:boolean}){const mapFa={PENDING:'در انتظار',IN_REVIEW:'در حال بررسی',APPROVED:'تأییدشده',NEEDS_REVISION:'نیازمند اصلاح'},mapEn={PENDING:'Pending',IN_REVIEW:'In review',APPROVED:'Approved',NEEDS_REVISION:'Needs revision'};return <span className={`rounded-full px-3 py-1 text-xs font-black ${status==='APPROVED'?'bg-green-50 text-green-700':status==='NEEDS_REVISION'?'bg-amber-50 text-amber-800':status==='IN_REVIEW'?'bg-blue-50 text-blue-700':'bg-[#f1f2f6] text-muted'}`}>{fa?mapFa[status]:mapEn[status]}</span>}
function defaultCriteria(skill:string):Record<string,number>{return skill==='speaking'?{fluency:0,lexicalResource:0,grammar:0,pronunciation:0}:{taskAchievement:0,coherence:0,lexicalResource:0,grammar:0}}
function criterionLabel(key:string,fa:boolean){const faMap:Record<string,string>={fluency:'روانی و انسجام',lexicalResource:'دامنه واژگان',grammar:'دقت و تنوع گرامری',pronunciation:'تلفظ',taskAchievement:'پاسخ‌گویی به تسک',coherence:'انسجام و پیوستگی'};return fa?(faMap[key]??key):key.replace(/([A-Z])/g,' $1').replace(/^./,letter=>letter.toUpperCase())}
function Empty({text}:{text:string}){return <div className="grid min-h-44 place-items-center rounded-2xl border border-dashed hairline p-8 text-center text-muted">{text}</div>}
function ErrorBox({message,retry}:{message:string;retry:()=>void}){return <div role="alert" className="mt-4 rounded-2xl bg-red-50 p-5 text-red-800">{message} <button onClick={retry} className="font-bold underline">Retry</button></div>}
