'use client';
import Link from'next/link';import{useQuery}from'@tanstack/react-query';import{Clock3,FileCheck,PlayCircle,RotateCcw}from'lucide-react';import{api,apiMessage}from'@/lib/api';import{jalali}from'@/lib/format';import{PageHeading,EmptyState}from'./page-heading';
type Attempt={id:string;status:string;createdAt:string;updatedAt:string;submittedAt?:string;expiresAt?:string;overallBand?:number;test:{titleFa:string;titleEn:string;level?:string;language?:string};scores?:unknown[]};
const labels:Record<string,string>={IN_PROGRESS:'در حال انجام',STARTED:'در حال انجام',UNDER_REVIEW:'در انتظار بررسی',SUBMITTED:'در انتظار بررسی',APPROVED:'نتیجه آماده',COMPLETED:'نتیجه آماده',EXPIRED:'منقضی‌شده'};
export function StudentTests(){const q=useQuery({queryKey:['attempt-history'],queryFn:()=>api<Attempt[]>('/tests/attempts/history')});return <><PageHeading title="آزمون‌ها" description="آزمون‌های فعال، روند پیشرفت و نتیجه‌های تأییدشده را یک‌جا دنبال کنید."/>{q.isLoading&&<div className="grid gap-4 md:grid-cols-2">{[1,2,3,4].map(i=><div key={i} className="skeleton h-64 rounded-2xl"/>)}</div>}{q.isError&&<div role="alert" className="rounded-2xl bg-red-50 p-5 text-red-700">{apiMessage(q.error,'دریافت آزمون‌ها ناموفق بود.')} <button onClick={()=>q.refetch()} className="font-bold underline">تلاش دوباره</button></div>}{q.data&&!q.data.length&&<EmptyState icon={<FileCheck/>} title="هنوز آزمونی ندارید" description="با انجام آزمون تعیین سطح، مسیر یادگیری متناسب با سطح شما ساخته می‌شود." action={<Link href="/placement" className="primary-button">شروع تعیین سطح</Link>}/>}<div className="grid gap-4 lg:grid-cols-2">{q.data?.map(a=>{const done=['APPROVED','COMPLETED'].includes(a.status),review=['UNDER_REVIEW','SUBMITTED'].includes(a.status);const progress=done?100:review?90:35;return <article key={a.id} className="panel-card p-5 md:p-6"><div className="flex items-start justify-between gap-4"><div><span className="text-xs font-bold text-indigo-600">{a.test.language??'آزمون زبان'} · {a.test.level??'تعیین سطح'}</span><h2 className="mt-2 text-lg font-black">{a.test.titleFa||a.test.titleEn}</h2></div><span className={`status-pill ${done?'status-success':review?'status-warning':'status-info'}`}>{labels[a.status]??a.status}</span></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><Meta label="تاریخ شروع" value={jalali(a.createdAt)}/><Meta label="آخرین فعالیت" value={jalali(a.updatedAt)}/><Meta label="تاریخ پایان" value={jalali(a.submittedAt??a.expiresAt)}/><Meta label="مدت آزمون" value="حدود ۶۰ دقیقه"/></div><div className="mt-5"><div className="mb-2 flex justify-between text-xs"><span className="text-muted">پیشرفت آزمون</span><b>{progress.toLocaleString('fa-IR')}٪</b></div><div className="h-2 overflow-hidden rounded-full bg-gray-100"><div className="h-full rounded-full bg-indigo-600" style={{width:`${progress}%`}}/></div></div><div className="mt-5 flex items-center justify-between"><span className="flex items-center gap-2 text-xs text-muted"><Clock3 size={15}/>{a.overallBand!=null?`نمره نهایی: ${a.overallBand.toLocaleString('fa-IR')}`:'آخرین ذخیره خودکار ثبت شده'}</span>{review?<button disabled className="secondary-button opacity-60">در انتظار بررسی</button>:<Link href={done?`/test/session?attempt=${a.id}`:`/test/session?attempt=${a.id}`} className="primary-button">{done?'مشاهده نتیجه':a.status==='IN_PROGRESS'?'ادامه آزمون':'شروع آزمون'} {done?<FileCheck size={17}/>:a.status==='IN_PROGRESS'?<RotateCcw size={17}/>:<PlayCircle size={17}/>}</Link>}</div></article>})}</div></>}
function Meta({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-gray-50 p-3"><small className="block text-muted">{label}</small><strong className="mt-1 block text-xs">{value}</strong></div>}
