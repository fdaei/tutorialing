'use client';

import {useState} from 'react';
import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';
import {Clock3,Filter,MessageCircle,Search,UserRound} from 'lucide-react';
import {api,apiMessage} from '@/lib/api';
import {useTranslations} from './locale-provider';
import {AsyncSearchSelect} from './async-search-select';

type Option={id:string;label:string;description?:string};
type TicketListItem={id:string;subject:string;category:string;priority:string;status:string;slaDueAt?:string;lastReplyAt?:string;updatedAt:string;user:{name?:string;phone:string;email?:string};assignedTo?:{id:string;name?:string;phone:string};replies:{body:string;author:{name?:string};createdAt:string}[];_count:{replies:number}};
type TicketPage={items:TicketListItem[];pagination:{page:number;pageSize:number;total:number;pages:number}};
type Reply={id:string;authorRole:string;direction:string;messageType:string;body:string;internal:boolean;createdAt:string;author:{name?:string;phone?:string}};
type TicketDetail=Omit<TicketListItem,'replies'>&{createdAt:string;replies:Reply[];statusHistory:{fromStatus?:string;toStatus:string;note?:string;createdAt:string;actor:{name?:string}}[];assignmentHistory:{createdAt:string;note?:string;actor:{name?:string}}[]};
const statuses=['OPEN','IN_PROGRESS','WAITING_USER','WAITING_SUPPORT','RESOLVED','CLOSED'];

export function TicketManager(){
 const {locale}=useTranslations(),fa=locale==='fa',qc=useQueryClient(),[scope,setScope]=useState('all'),[status,setStatus]=useState(''),[priority,setPriority]=useState(''),[search,setSearch]=useState(''),[page,setPage]=useState(1),[selectedId,setSelectedId]=useState<string>(),[user,setUser]=useState<Option|null>(null),[assigneeFilter,setAssigneeFilter]=useState<Option|null>(null);
 const me=useQuery({queryKey:['panel-me'],queryFn:()=>api<{permissions:string[]}>('/users/me'),retry:false}),canManage=Array.isArray(me.data?.permissions)&&me.data.permissions.includes('tickets.manage');
 const params=new URLSearchParams({page:String(page),pageSize:'20',scope:scope==='all'?'':scope,status,priority,search});if(user)params.set('userId',user.id);if(assigneeFilter)params.set('assignedToId',assigneeFilter.id);
 const list=useQuery({queryKey:['tickets',scope,status,priority,search,page,user?.id,assigneeFilter?.id],queryFn:()=>api<TicketPage>(`/support/tickets?${params.toString()}`)});
 const detail=useQuery({queryKey:['ticket-detail',selectedId],queryFn:()=>api<TicketDetail>(`/support/tickets/${selectedId}`),enabled:!!selectedId});
 const refresh=async()=>{await Promise.all([qc.invalidateQueries({queryKey:['tickets']}),qc.invalidateQueries({queryKey:['ticket-detail',selectedId]})])};
 return <div className="grid gap-6"><section className="rounded-3xl border hairline bg-white p-5"><div className="grid gap-3 lg:grid-cols-[1.2fr_repeat(3,.7fr)]"><label className="flex items-center gap-2 rounded-xl border hairline px-4"><Search size={17}/><input value={search} onChange={event=>{setSearch(event.target.value);setPage(1)}} placeholder={fa?'جستجو در موضوع، متن، نام، موبایل یا ایمیل…':'Search subject, message, name, phone, or email…'} className="w-full py-3 outline-none"/></label><select value={scope} onChange={event=>{setScope(event.target.value);setPage(1)}} className="input"><option value="all">{fa?'همه تیکت‌ها':'All tickets'}</option><option value="mine">{fa?'اختصاص‌یافته به من':'Assigned to me'}</option><option value="unassigned">{fa?'بدون مسئول':'Unassigned'}</option></select><select value={status} onChange={event=>{setStatus(event.target.value);setPage(1)}} className="input"><option value="">{fa?'همه وضعیت‌ها':'All statuses'}</option>{statuses.map(value=><option key={value}>{value}</option>)}</select><select value={priority} onChange={event=>{setPriority(event.target.value);setPage(1)}} className="input"><option value="">{fa?'همه اولویت‌ها':'All priorities'}</option><option value="urgent">urgent</option><option value="high">high</option><option value="normal">normal</option><option value="low">low</option></select></div><div className="mt-3 grid gap-3 md:grid-cols-2"><AsyncSearchSelect entity="users" value={user} onChange={value=>{setUser(value);setPage(1)}} placeholder={fa?'فیلتر کاربر':'Filter user'}/><AsyncSearchSelect entity="support-agents" value={assigneeFilter} onChange={value=>{setAssigneeFilter(value);setPage(1)}} placeholder={fa?'فیلتر مسئول':'Filter assignee'}/></div></section><div className="grid gap-6 xl:grid-cols-[410px_minmax(0,1fr)]"><section className="rounded-3xl border hairline bg-white p-4"><div className="flex items-center justify-between px-2"><h2 className="text-xl font-black">{fa?'فهرست تیکت‌ها':'Ticket list'}</h2><span className="rounded-full bg-lavender px-3 py-1 text-xs font-black text-purple">{list.data?.pagination.total??0}</span></div>{list.isLoading?<div className="mt-4 grid gap-3">{Array.from({length:5},(_,i)=><div key={i} className="skeleton h-32 rounded-2xl"/>)}</div>:list.isError?<Error message={apiMessage(list.error,fa?'فهرست تیکت‌ها دریافت نشد.':'Could not load tickets.')} retry={()=>list.refetch()}/>:list.data?.items.length?<div className="mt-4 grid gap-3">{list.data.items.map(ticket=><button key={ticket.id} onClick={()=>setSelectedId(ticket.id)} className={`rounded-2xl border p-4 text-start ${selectedId===ticket.id?'border-purple bg-lavender/35':'hairline hover:border-purple'}`}><div className="flex items-start justify-between gap-3"><strong className="line-clamp-1">{ticket.subject}</strong><TicketStatus value={ticket.status} fa={fa}/></div><p className="mt-2 text-sm text-muted">{ticket.user.name||ticket.user.phone} · {ticket.category} · {priorityLabel(ticket.priority,fa)}</p><p className="mt-2 line-clamp-2 text-sm">{ticket.replies[0]?.body}</p><div className="mt-3 flex items-center justify-between text-xs text-muted"><span>{ticket.assignedTo?.name||(fa?'بدون مسئول':'Unassigned')}</span><span>{ticket._count.replies} {fa?'پیام':'messages'}</span></div></button>)}</div>:<Empty text={fa?'تیکتی با این فیلتر پیدا نشد.':'No tickets match these filters.'}/>}<div className="mt-4 flex items-center justify-between"><button disabled={page<=1} onClick={()=>setPage(value=>value-1)} className="rounded-xl border hairline px-4 py-2 disabled:opacity-30">{fa?'قبلی':'Previous'}</button><span>{page} / {Math.max(1,list.data?.pagination.pages??1)}</span><button disabled={page>=(list.data?.pagination.pages??1)} onClick={()=>setPage(value=>value+1)} className="rounded-xl border hairline px-4 py-2 disabled:opacity-30">{fa?'بعدی':'Next'}</button></div></section><section className="rounded-3xl border hairline bg-white p-6">{selectedId?(detail.isLoading?<div className="skeleton h-[600px] rounded-2xl"/>:detail.data?<TicketDetailView ticket={detail.data} fa={fa} refresh={refresh} canManage={canManage}/>:<Error message={apiMessage(detail.error,fa?'جزئیات تیکت دریافت نشد.':'Could not load ticket details.')} retry={()=>detail.refetch()}/>):<Empty text={fa?'یک تیکت را انتخاب کنید.':'Select a ticket.'}/>}</section></div><style jsx global>{`.input{width:100%;border:1px solid rgba(16,29,53,.14);border-radius:.9rem;padding:.8rem 1rem;background:white;outline:none}.input:focus{border-color:#7257d9;box-shadow:0 0 0 4px rgba(114,87,217,.08)}`}</style></div>
}
function TicketDetailView({ticket,fa,refresh,canManage}:{ticket:TicketDetail;fa:boolean;refresh:()=>Promise<void>;canManage:boolean}){
 const [status,setStatus]=useState(ticket.status),[assignee,setAssignee]=useState<Option|null>(ticket.assignedTo?{id:ticket.assignedTo.id,label:ticket.assignedTo.name||ticket.assignedTo.phone,description:ticket.assignedTo.phone}:null),[body,setBody]=useState(''),[internal,setInternal]=useState(false),[note,setNote]=useState('');
 const statusMutation=useMutation({mutationFn:()=>api(`/support/tickets/${ticket.id}/status`,{method:'PATCH',body:JSON.stringify({status,note:note||undefined})}),onSuccess:refresh,onError:()=>setStatus(ticket.status)});
 const assignment=useMutation({mutationFn:()=>api(`/support/tickets/${ticket.id}/assignment`,{method:'PATCH',body:JSON.stringify({assignedToId:assignee?.id??null,note:note||undefined})}),onSuccess:refresh});
 const reply=useMutation({mutationFn:()=>api(`/support/tickets/${ticket.id}/replies`,{method:'POST',body:JSON.stringify({body,internal})}),onSuccess:async()=>{setBody('');setInternal(false);await refresh()}});
 return <div><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-bold text-purple">{ticket.category} · {priorityLabel(ticket.priority,fa)}</p><h2 className="mt-2 text-2xl font-black">{ticket.subject}</h2></div><TicketStatus value={ticket.status} fa={fa}/></div><dl className="mt-6 grid gap-4 rounded-2xl bg-[#f7f8fc] p-5 sm:grid-cols-2 lg:grid-cols-4"><Meta label={fa?'کاربر':'User'} value={`${ticket.user.name||'—'} · ${ticket.user.phone}`}/><Meta label={fa?'ایمیل':'Email'} value={ticket.user.email||'—'}/><Meta label={fa?'مسئول':'Assignee'} value={ticket.assignedTo?.name||ticket.assignedTo?.phone||(fa?'بدون مسئول':'Unassigned')}/><Meta label="SLA" value={ticket.slaDueAt?format(ticket.slaDueAt,fa):'—'}/><Meta label={fa?'ایجاد':'Created'} value={format(ticket.createdAt,fa)}/><Meta label={fa?'آخرین پاسخ':'Last reply'} value={ticket.lastReplyAt?format(ticket.lastReplyAt,fa):'—'}/></dl>{canManage&&<><div className="mt-6 grid gap-4 md:grid-cols-2"><div><label className="mb-2 block text-sm font-black">{fa?'تغییر وضعیت':'Change status'}</label><div className="flex gap-2"><select value={status} onChange={event=>setStatus(event.target.value)} className="input">{statuses.map(value=><option key={value}>{value}</option>)}</select><button disabled={statusMutation.isPending} onClick={()=>statusMutation.mutate()} className="rounded-xl bg-navy px-4 font-bold text-white">{fa?'ذخیره':'Save'}</button></div></div><div><label className="mb-2 block text-sm font-black">{fa?'ارجاع مسئول':'Assign owner'}</label><div className="flex gap-2"><AsyncSearchSelect entity="support-agents" value={assignee} onChange={setAssignee} className="min-w-0 flex-1"/><button disabled={assignment.isPending} onClick={()=>assignment.mutate()} className="rounded-xl bg-navy px-4 font-bold text-white">{fa?'ثبت':'Assign'}</button></div></div></div><input value={note} onChange={event=>setNote(event.target.value)} className="input mt-3" placeholder={fa?'یادداشت دلیل تغییر وضعیت یا ارجاع (اختیاری)':'Optional status/assignment note'}/>{(statusMutation.isError||assignment.isError)&&<p role="alert" className="mt-3 rounded-xl bg-red-50 p-3 text-red-800">{apiMessage(statusMutation.error||assignment.error,fa?'تغییر ذخیره نشد و مقدار قبلی بازگردانده شد.':'The change was not saved and the previous value was restored.')}</p>}</>}<div className="mt-8 space-y-4">{ticket.replies.map(message=><Message key={message.id} message={message} fa={fa}/>)}</div><div className="mt-7 rounded-2xl border hairline p-4"><textarea value={body} onChange={event=>setBody(event.target.value)} className="input min-h-28" placeholder={internal?(fa?'یادداشت داخلی؛ کاربر آن را نمی‌بیند.':'Internal note; hidden from the user.'):(fa?'پاسخ به کاربر…':'Reply to the user…')}/>{canManage&&<label className="mt-3 flex items-center gap-2 text-sm"><input type="checkbox" checked={internal} onChange={event=>setInternal(event.target.checked)} className="accent-purple"/>{fa?'یادداشت داخلی':'Internal note'}</label>}{reply.isError&&<p className="mt-3 text-sm text-red-700">{apiMessage(reply.error,fa?'ارسال پاسخ ناموفق بود.':'Could not send the reply.')}</p>}<button disabled={!body.trim()||reply.isPending} onClick={()=>reply.mutate()} className="brand-gradient mt-4 rounded-xl px-6 py-3 font-black text-white disabled:opacity-40">{reply.isPending?(fa?'در حال ارسال…':'Sending…'):(internal?(fa?'ثبت یادداشت داخلی':'Save internal note'):(fa?'ارسال پاسخ':'Send reply'))}</button></div></div>
}
function Message({message,fa}:{message:Reply;fa:boolean}){const kind=message.messageType==='USER_MESSAGE'?(fa?'پیام کاربر':'User message'):message.messageType==='INTERNAL_NOTE'?(fa?'یادداشت داخلی':'Internal note'):message.messageType==='SYSTEM'?(fa?'پیام سیستمی':'System message'):(fa?'پاسخ پشتیبان':'Support reply');return <article className={`rounded-2xl border p-4 ${message.messageType==='INTERNAL_NOTE'?'border-amber-200 bg-amber-50':message.messageType==='SYSTEM'?'border-dashed bg-[#f6f7fa]':message.direction==='INBOUND'?'border-blue/20 bg-blue/5':'border-purple/20 bg-lavender/30'}`}><div className="flex flex-wrap items-center justify-between gap-2"><strong className="flex items-center gap-2"><MessageCircle size={16}/>{kind} · {message.author.name||message.authorRole}</strong><span className="text-xs text-muted">{format(message.createdAt,fa)}</span></div><p className="mt-3 whitespace-pre-wrap leading-7">{message.body.startsWith('STATUS:')?(fa?'وضعیت تیکت به‌روزرسانی شد.':'Ticket status was updated.'):message.body}</p><p className="mt-3 text-xs text-muted">{fa?'نقش فرستنده':'Author role'}: {message.authorRole} · {message.direction}</p></article>}
function Meta({label,value}:{label:string;value:string}){return <div><dt className="text-xs text-muted">{label}</dt><dd className="mt-1 font-bold">{value}</dd></div>}
function TicketStatus({value,fa}:{value:string;fa:boolean}){const map:Record<string,string>={OPEN:'باز',IN_PROGRESS:'در حال رسیدگی',WAITING_USER:'منتظر کاربر',WAITING_SUPPORT:'منتظر پشتیبانی',RESOLVED:'حل‌شده',CLOSED:'بسته'};return <span className="rounded-full bg-lavender px-3 py-1 text-xs font-black text-purple">{fa?(map[value]??value):value.replaceAll('_',' ')}</span>}
function priorityLabel(value:string,fa:boolean){const map:Record<string,string>={urgent:'فوری',high:'زیاد',normal:'عادی',low:'کم'};return fa?(map[value]??value):value}
function format(value:string,fa:boolean){return new Intl.DateTimeFormat(fa?'fa-IR-u-ca-persian':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value))}
function Empty({text}:{text:string}){return <div className="grid min-h-40 place-items-center rounded-2xl border border-dashed hairline p-8 text-center text-muted">{text}</div>}
function Error({message,retry}:{message:string;retry:()=>void}){return <div role="alert" className="mt-4 rounded-2xl bg-red-50 p-5 text-red-800">{message} <button onClick={retry} className="font-bold underline">Retry</button></div>}
