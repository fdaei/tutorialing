import{test,expect}from'@playwright/test';test('public discovery and auth are accessible',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{level:1})).toBeVisible();await page.goto('/teachers');await expect(page.getByRole('heading',{level:1})).toContainText('مدرسی');await page.goto('/auth');await expect(page.getByLabel('شماره موبایل')).toBeVisible()});test('mobile layout has no horizontal overflow',async({page})=>{await page.setViewportSize({width:360,height:800});await page.goto('/placement');const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false)});
