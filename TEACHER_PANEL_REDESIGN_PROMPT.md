# پرامپت اجرایی بازطراحی پنل مدرس

نقش شما: طراح ارشد محصول و مهندس فرانت‌اند یک پلتفرم آموزش زبان فارسی/انگلیسی با رابط RTL/LTR هستید.

پنل مدرس موجود را با حفظ APIها و قابلیت‌های فعلی بازطراحی و پیاده‌سازی کن. هدف اصلی کاهش بار شناختی است: مدرس باید در کمتر از ۵ ثانیه بفهمد کلاس بعدی چه زمانی است، چه کاری نیاز به اقدام دارد و چقدر درآمد قابل تسویه دارد.

الزامات:

1. منوی ۱۷ آیتمی را به ۶ مقصد اصلی کاهش بده: نمای کلی، پروفایل و تأیید، برنامه کاری، کلاس‌ها، زبان‌آموزان، مالی. قابلیت‌های کم‌تکرار را زیر «بیشتر» قرار بده.
2. صفحات تکراری پروفایل، زبان، تخصص، مدارک و ویدیو را در یک صفحه با تب‌های واضح ادغام کن. اطلاعات یکسان را در چند مسیر رندر نکن.
3. «برنامه هفتگی» و «تقویم و مسدودی‌ها» را یک تجربه واحد کن.
4. داشبورد تصمیم‌محور بساز: کارت کلاس بعدی، تعداد کلاس‌های پیش رو، کلاس‌های برگزارشده، زبان‌آموزان، موجودی قابل تسویه، کارهای ضروری و دسترسی‌های سریع.
5. صفحه مالی کامل بساز: موجودی قابل تسویه، درآمد خالص کل، تسویه‌شده، و جدول ریز درآمد شامل مبلغ ناخالص، کارمزد، سهم مدرس و وضعیت. اعداد مالی را محلی‌سازی و واحد را دقیق نمایش بده.
6. داده خام API، enum انگلیسی و کلیدهای فنی را به کاربر نشان نده. حالت‌های loading، error و empty باید راهنمای عملی داشته باشند.
7. سلسله‌مراتب بصری، فاصله‌گذاری، کنتراست، اندازه اهداف لمسی و responsive بودن را رعایت کن. طراحی باید برای فارسی RTL و انگلیسی LTR درست باشد.
8. هیچ API یا workflow موجود را حذف نکن؛ مسیرهای قدیمی را به مقصد ادغام‌شده redirect کن تا bookmarkها نشکنند.
9. از کامپوننت‌ها و توکن‌های فعلی پروژه استفاده کن و در پایان lint، typecheck و build را اجرا کن.

معیار پذیرش: هیچ فرم پروفایل تکراری نباشد، منوی اصلی حداکثر ۷ آیتم داشته باشد، صفحه مالی در حالت دارای داده و بدون داده قابل فهم باشد، و مدرس بدون آموزش بتواند کلاس بعدی و اقدام بعدی خود را پیدا کند.
